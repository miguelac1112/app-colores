apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: backend-db
  name: backend-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-db
  strategy: {}
  template:
    metadata:
      labels:
        app: backend-db
    spec:
      containers:
      - image: "{{ .Values.baseDeDatos.imagen }}"
        name: redis
        resources: {}
status: {}
